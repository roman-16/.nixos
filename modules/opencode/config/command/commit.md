---
description: execute the version control flow (s, c or p)
---

Execute the Version Control flow from `~/.nixos/modules/opencode/files/update-agents/AGENTS-universal.md`. Use `cat` to read the file.
Skip this step: Ask: "Type `y` to start committing"

User input:
$ARGUMENTS
