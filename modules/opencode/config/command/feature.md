---
description: create a new feature
---

Execute the Workflow from `~/.nixos/modules/opencode/files/update-agents/AGENTS-universal.md` and create the plan file. Use `cat` to read the file.

User input:
$ARGUMENTS
