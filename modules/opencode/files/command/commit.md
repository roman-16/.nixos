---
description: execute the version control flow (s, c, or p)
---

Execute the Version Control flow from `AGENTS.md`.
Skip this step: Use `question_tool`: "Start committing"

User input (if the input already has `s`, `c`, or `p`, execute the appropriate steps):
$ARGUMENTS
