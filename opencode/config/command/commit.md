---
description: Execute the version control flow
agent: build
---

Execute the Version Control flow from `~/.nixos/opencode/config/command/update-agents.md`. Use `cat` to read the file.
Skip this step: Ask: "Type `y` to start committing"

User input:
$ARGUMENTS
