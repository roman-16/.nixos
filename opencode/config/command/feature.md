---
description: Create a new feature
agent: build
---

Execute the Workflow from `~/.nixos/opencode/config/command/update-agents.md` and create the plan file. Use `cat` to read the file.

User input:
$ARGUMENTS
