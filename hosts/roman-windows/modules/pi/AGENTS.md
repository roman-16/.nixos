## Environment

- You are running inside **NixOS-WSL** (WSL2) on a **Windows 11** machine: the Linux userland is NixOS, the host OS is Windows 11. Windows tooling is reachable via interop (`powershell.exe`, `winget.exe`, `cmd.exe`, `wslpath`) and the Windows filesystem is mounted under `/mnt/c`.
